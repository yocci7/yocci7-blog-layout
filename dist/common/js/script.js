const sort_buttons = document.querySelectorAll(".sidebar__article__sort__button");
const sort_article = document.querySelectorAll(".article__section");
console.log(sort_buttons);
console.log(sort_article);
